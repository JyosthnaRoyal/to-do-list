<%- include('header'); -%>
    <!-- Header layout -->

    <!-- listTitle is taken from app.js  Passing data from server to template -->
    <!-- "<%" and "%>"" are scriplet tags ;we need to write this tag for only javascript code -->
    <div class="box" id="heading">
        <h1>
            <%= listTitle %>
        </h1>
    </div>

    <!-- newListItems is taken from app.js ->Passing data from server to template -->
    <div class="box">
        <%for(let i=0;i<newListItems.length;i++){%>
            <div class="item">
                <input type="checkbox">
                <p>
                    <%= newListItems[i] %>
                </p>
            </div>

            <% } %>
                <form class="item" action="/" method="post">
                    <input type="text" name="newitem" placeholder="New item" autocomplete="off">
                    <button type="submit" name="list" value="<%= listTitle %>">+</button>

                </form>
    </div>
    <%- include('footer'); -%>
        <!-- Footer layout -->







        </ul>